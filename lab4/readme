实验内容简介：
	实验测试可以直接使用写好的make，报告中的测试代码test.c也在这里
	可以直接make,然后./calc test.c进行检查。
	另一方面也可以自行编写测试程序，但由于我能力有限，只完成了部分
	语句的翻译，请在编写测试程序之前，先看一下下面的内容，确定一下什
	么样的程序是能够被翻译的。
	实验程序的语法主要参考实验三。
我做了什么:
	1.基本的函数框架:为了能够和实验三的语法分析接轨，我简单的完成了一个函数框架
	但是没有完成函数调用和参数传递,相当于只能完成类似 type functionName(){语句}
	这样的内容，基本只是用于通过语法分析而做的。

	2.初始化语句,赋值语句:除了不支持函数和数组变量外，对于普通的变量而言，只要
	符合实验三语法的语句都能够分析。

	3.if语句:if语句的条件判断里只支持CMP表达式，内部的语句支持初始化，赋值，return
	if,while。

	4.while语句:条件判断只支持CMP表达式，内部语句支持初始化，赋值，return，if，while

	5.return语句:return语句右侧的表达式支持addExpr和CmpExpr以及"return;"。没有
	写关于数组变量和函数返回值的部分。